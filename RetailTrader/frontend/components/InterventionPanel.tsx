export function InterventionPanel() {
  return <section className="builder-note"><strong>Deterministic intervention</strong><span>RetailTrader caps position size, cash, and turnover before simulation. No real-order path exists.</span></section>;
}
